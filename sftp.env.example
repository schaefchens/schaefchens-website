# Copy to sftp.env and fill in. Gitignored — never commit the copy.
#
# The account is jailed to the document root of https://schaefchens.de, so the
# remote "/" in scripts/deploy.sh is the web root.
SFTP_SERVER=user@www99.your-server.de
SFTP_PASSWD=
